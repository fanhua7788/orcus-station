# ❄️ ORCUS STATION · 极地科考站失联日志

> 你偶然打开了一台失联极地科考站「奥库斯站」的公用终端。  
> 日志停在最后一分钟，最后一条写着：  
> **「我听见冰下面有人敲门。」**

这是一款网页解谜游戏（ARG），不需要安装任何东西，用浏览器就能玩。

---

## 🎮 开始玩

打开 `index.html`，你就是这台终端前的人了。

- **从主控台的日志开始**，顺着线索一路查下去。
- 全程共 **9 关**，密码被拆成 **5 块档案残片**，散落在各处。
- 残片会在你到达对应页面时**自动记录**，页面顶部的进度条会亮起：`O R C U S`。
- 解谜全部是**轻量级、页面内即可完成**（悬停涂黑字、点按钮提亮、拖动滑块），无需任何外部工具。
- 主线靠 **剧情文本推进**——建议放慢阅读。算上驻站日志、职员档案、勘察记录、声学日志、幸存者证词，是 **1 小时以上**的完整体验。

> 提示：F12 开发者控制台里，每个页面都留了一句悄悄话。

---

## 🧭 游戏里有哪些地方

| 页面 | 说明 |
|------|------|
| 主控台 | 失联日志，一切的开始（含站史 + 值守员手记） |
| 环境监测 | 气象数据 + 声学记录 + 页面内读数解析 |
| 摄像头 | 6 路监控画面（CAM-03 点「提亮」即可看到红字代号） |
| 档案库 | 需要密码的最终入口 |
| 解码工具 | 可选的分析终端（主线不依赖它） |
| 补全档案 | 建站勘察记录（站史深挖） |
| 声学日志 | 科瓦奇失踪前的手写日志 |
| 幸存者证词 | 五名留守人员的封存证词 |

还有几个页面**不在导航栏里**——需要靠解谜找到它们。

---

## 🔧 工具与帮助

### 💡 卡住了怎么办
每个关卡页面底部都有一个 **「💡 卡住了？」** 提示框，分三级：
1. **轻微提示** —— 只给方向
2. **明确提示** —— 告诉你关键步骤
3. **直接答案** —— 实在不行就点开

按需点开，不会破坏其他玩家体验。

### 🔬 图像分析终端（解码工具）
在「解码工具」页可以：
- 上传图片，调节**亮度 / 对比度 / Gamma / 色阶**
- 一键「色阶增强」，让暗处藏着的文字显形
- 提取像素**最低比特（LSB）**，自动搜索隐藏签名

### 📊 音频频谱
「环境监测」页的声学记录支持**波形可视化**和**自动解码**——不用听，看波形就能读出摩尔斯码。

### 🧩 残片进度
五块残片 `O / R / C / U / S` 会随进度自动点亮（存在浏览器 localStorage 里）。
想从头再来？在控制台输入 `resetProgress()`。

---

## ⁉️ 常见问题

- **听不到声音？** 浏览器需要用户先点击「播放」，这是浏览器的自动播放策略。
- **图片里的字看不清？** 用解码工具的「一键色阶增强」。
- **卡在密码门？** 五块残片按编号顺序拼，就是密码。
- **进度条不亮？** 进度存在浏览器 localStorage，换浏览器/清缓存会重置。

---

# ⚠️ 以下内容含剧透（出题人专区）

<details>
<summary>点击展开完整答案与关卡链</summary>

| # | 页面 | 谜题（全部页面内即解） | 答案 |
|---|------|------|------|
| 1 | 主控台 | 日志涂黑字（悬停/Ctrl+A） | CHECK THE LOWEST TEMP |
| 2 | 环境监测 | 07-03 行 67/65/77/51 → 页面内「读数解析」 | CAM 3 |
| 3 | 摄像头 | CAM-03 点「🔦 提亮」见 RADIO；残片自动收集 | RADIO + 残片 O |
| 4 | 通讯记录（隐藏） | 凯撒 -3（有「⚡ 自动破解」按钮） | PERSONNEL + 残片 R |
| 5 | 员工档案（隐藏） | 失踪者工号 7341；涂黑字 POWER | 残片 C |
| 6 | 电源（隐藏） | 断路器序列 7341 | /404.html + 残片 U |
| 7 | 404（隐藏） | 最后一页 | 残片 S |
| 8 | 档案库 | 五块残片拼密码 | ORCUS |
| 9 | 结局 | 输入你的名字 | 它敲的是你的名字 |

**残片顺序：O → R → C → U → S = ORCUS**

### 资源生成（可复现）

```bash
node tools/generate.mjs
```

- `assets/img/cam1..6.png`：程序化渲染的监控画面
- `cam3.png`：视觉隐写 `RADIO` + LSB 隐写 `O`
- `assets/audio/signal.wav`：摩尔斯 `CAM 3` 敲击声

### 目录结构

```
arg-orcus/
├── index.html        # 1 失联日志
├── climate.html      # 2 环境监测（含频谱可视化）
├── cameras.html      # 3 摄像头（CAM-03 可页面内提亮）
├── radio.html        # 4 通讯记录（隐藏）
├── personnel.html    # 5 员工档案（隐藏，含全员档案）
├── power.html        # 6 电源管理（隐藏）
├── 404.html          # 7 404 藏页（隐藏）
├── vault.html        # 8 密码门
├── final.html        # 9 结局
├── decoder.html      # 图像分析终端（可选）
├── survey.html       # 补全档案 · 建站勘察（剧情深挖）
├── kovach.html       # 声学日志 · 科瓦奇手写本（剧情深挖）
├── testimony.html    # 幸存者证词 · 封存（剧情深挖）
├── assets/
│   ├── style.css / app.js
│   ├── img/cam1..6.png
│   └── audio/signal.wav
└── tools/generate.mjs
```

</details>

---

*不要回应。*
